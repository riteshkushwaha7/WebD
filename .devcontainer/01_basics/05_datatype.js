// primitive

const outsideTemp = null;
let userEmail;

const id = Symbol('123');
const anotherId = Symbol('123');
console.log(id==anotherId)

// bigINt, string, number, boolean, symbol, null, undefined 7 primitive data types

// Non primitve (reference datatye)

// function objects array

const myFunction = function(){
    console.log(nothing);
}
console.log(typeof myFunction);

// false
// function